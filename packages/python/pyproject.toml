[build-system]
requires = ["setuptools>=70.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chronow-python"
version = "0.1.0"
description = "Chronow deterministic temporal engine Python bindings"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["chronow"]
